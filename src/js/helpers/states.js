export const recorderState = {
    STARTED : "STARTED",
    PAUSED : "PAUSED",
    STOPPED : "STOPPED"
}

export const sessionState = {
    STARTED : "STARTED",
    STOPPED : "STOPPED"
}

export const sensingState = {
    STARTED : "STARTED",
    STOPPED : "STOPPED"
}